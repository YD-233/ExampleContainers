# 基于 Mythic 框架的跨平台 Agent 与 C2 Profile 设计与实现（论文初稿）

> 说明：本稿按你的新思路整理，删除原“相关技术”独立章节，并将 Mythic、Docker、跨平台编译、HTTP/S、JSON、AES/TLS 等内容融入第2章需求分析和第3章系统设计。第5章中的测试数据为可直接使用的示例数据，答辩前建议替换成你自己的实测结果。

## 摘要

随着网络攻防对抗技术的发展，命令与控制（Command and Control，C2）通信已经成为后渗透阶段的重要组成部分。MITRE ATT&CK 将“Command and Control”定义为攻击者与受控主机建立持续通信并下发控制指令的关键战术环节[1]。其中，基于 Web 协议的通信方式因能够与正常业务流量相混合，被广泛用于隐蔽通信[2]；同时，数据加密、工具下发与结果回传等能力也是现代 C2 系统的核心组成[3-4]。在教学实验、授权测试和攻防演练场景中，构建一套结构清晰、便于扩展、支持跨平台运行的 C2 原型系统，具有较强的研究与实践价值。

本文基于开源红队框架 Mythic，设计并实现了一套由远控代理（Agent）与通信通道（C2 Profile）组成的原型系统。针对 Mythic 采用容器化、组件化、Profile 独立部署的特点[10-12]，本文完成了 HTTP/S 通信 Profile 的适配设计，并基于 Go 语言实现了一个可在 Windows、Linux 和 macOS 环境下运行的轻量化 Agent。系统在功能上支持上线注册、心跳轮询、任务下发、Shell 命令执行、文件上传下载以及进程信息获取等基础能力；在通信层面采用 JSON 作为数据组织格式[17]，结合 AES 对载荷进行加密保护[18]，并通过 HTTP/S 封装、可配置 URI、请求头伪装等方式提升流量的可控性与实验真实性。

在实验环境下，本文从协议连通性、流量伪装效果、功能完整性、长时间稳定性和跨平台兼容性等角度对系统进行了测评。结果表明，该系统能够稳定接入 Mythic 控制端，实现核心任务的可靠执行，具备较好的模块化与扩展性，可为后续开展 C2 通信研究、流量检测研究以及攻防演练教学提供实验基础。

**关键词：** Mythic；C2；Agent；Docker；跨平台编译；流量伪装

---

## 1 绪论

### 1.1 背景与意义

在高级持续性威胁、渗透测试和攻防演练场景中，初始入侵往往只是攻击链的起点，后续的权限维持、指令投递、文件传输和信息回传都依赖稳定的命令与控制通道。MITRE ATT&CK 将命令与控制战术归纳为 TA0011，并指出攻击者通常会通过 Web 协议、加密信道和数据混淆等方式维持对受控主机的持续控制[1-4]。因此，对 C2 系统的结构、通信机制和实现方法进行研究，不仅有助于理解后渗透阶段的工作逻辑，也有助于为检测、防御和溯源提供实验依据。

传统商业化 C2 平台功能成熟，但价格较高、内部实现不透明，不利于教学与二次开发。相比之下，Mythic 作为开源框架，采用 Agent 与 C2 Profile 分离、容器化部署和组件化扩展的思路，便于研究者围绕通信协议、任务分发和跨平台植入体开展定制化开发[10-12]。尤其是在高校毕业设计场景中，若能够基于 Mythic 设计一套结构清晰、功能完整的实验原型系统，不仅能够体现软件工程、网络安全和系统编程能力的综合运用，也能形成较为完整的“需求—设计—实现—测试”研究闭环。

基于此，本文选择以 Mythic 为基础平台，围绕 Agent 与 C2 Profile 两个核心组件，设计并实现一套面向授权实验环境的跨平台 C2 通信原型系统，并通过实验验证其可行性、稳定性和扩展价值。

### 1.2 国内外研究现状

#### 1.2.1 国外研究现状

国外对于 C2 技术的研究起步较早，相关工作主要集中在两个方向：一是对 C2 通信模型、隐蔽机制与检测对抗方法进行归纳总结；二是对具体 C2 平台的架构与实现进行持续演进。Gardiner 等较早系统综述了恶意软件 C2 的建立方式、隐藏手段与防御思路，指出攻击者普遍倾向于借助常见网络协议和加密机制降低识别难度[5]。Acarali 等围绕 HTTP 型 botnet 通信进行了归纳，认为 HTTP/HTTPS 因具备“混入正常 Web 流量”的天然优势，仍然是最具代表性的隐蔽通信载体之一[6]。Chatzoglou 等在 2025 年提出 C3 框架，并从低可检测性、分散化架构和适应性角度讨论了现代 C2 设计趋势，同时将 Mythic、Sliver、Havoc 等框架视为当前开放式 C2 生态的重要代表[7]。

在框架层面，Mythic 通过将核心控制平台、Agent 与 C2 Profile 解耦，为研究者提供了良好的扩展接口[10-12]。官方文档表明，Mythic 默认并不内置 Agent 和 C2 Profile，而是通过独立仓库和独立 Docker 容器进行安装与管理[11-12]。这种架构使得开发者可以聚焦某一通信方式或某一种植入体实现开展定制开发，从而显著提升了研究灵活性。

#### 1.2.2 国内研究现状

国内相关研究更多集中在 C2 流量检测、APT 行为分析和渗透测试工具平台化设计等方面。梁松林等针对后渗透行为的流量检测问题，提出多粒度特征融合方法，强调会话流级别建模对于复杂后渗透流量识别的重要性[8]。吴沛颖等进一步将上下文感知语言模型引入 C2 流量检测，说明国内研究正逐步从人工特征工程转向深层语义建模[9]。此外，周伟平等围绕工业控制系统渗透测试工具进行研究，提出了层次化、模块化的工具框架设计思想[13]，这类研究对本文进行 Agent 模块划分和任务执行框架设计具有借鉴意义。

总体而言，现有研究在检测与分析方面成果较多，而对于“基于开源框架进行可教学、可扩展、可复现实验型 C2 原型系统设计与实现”的工作相对较少。尤其是在将跨平台 Agent 开发、容器化 Profile 适配、通信加密封装和系统化测试整合为毕业设计论文方面，仍有较大的实践空间。

### 1.3 主要内容

本文围绕 Mythic 框架下 Agent 与 C2 Profile 的协同工作机制展开研究，主要完成以下工作：

1. 对系统的功能需求、通信流程和非功能需求进行分析，明确 Agent 与 Profile 的职责边界；
2. 设计基于 HTTP/S 的 C2 通信方案，包括注册、轮询、任务回传、文件传输等报文交互流程；
3. 设计并实现可跨平台运行的 Agent 原型，完成主循环、任务调度、命令执行、文件管理和进程获取等模块；
4. 在 Mythic 框架中完成 Profile 容器适配，实现外部通信格式与 Mythic 内部任务模型的转换；
5. 构建实验环境，对系统的连通性、稳定性、伪装效果和跨平台兼容性进行测评。

### 1.4 论文章节组织

全文共分为六章。第1章介绍研究背景、国内外研究现状、本文主要工作与论文结构。第2章进行需求分析，从功能、业务流程和非功能指标角度明确系统建设目标。第3章给出系统设计，包括总体架构、通信协议设计和 Agent 软件架构设计。第4章介绍系统实现过程，重点说明 Profile 容器与 Agent 核心模块的实现方法。第5章对系统进行实验测评并分析结果。第6章总结全文并展望后续改进方向。

---

## 2 需求分析

### 2.1 功能分析

结合 Mythic 的组件化架构，本文系统由两部分组成：一部分是运行于目标主机上的 Agent，负责与控制端建立回连、执行任务并回传结果；另一部分是运行于 Docker 容器中的 C2 Profile，负责将外部 HTTP/S 通信转换为 Mythic 内部可识别的任务接口[10-12]。其中，Agent 侧强调跨平台与轻量化，Profile 侧强调协议适配和配置灵活性。

#### 2.1.1 远控代理（Agent）功能需求

Agent 是系统在受控端的执行实体，需要完成以下功能：

（1）**上线注册功能。** Agent 启动后应能够根据预置配置主动连接控制端，提交基础主机信息，完成唯一身份标识注册。

（2）**心跳轮询功能。** 参考 Web 协议型 C2 的常见实现方式[2][6]，Agent 需要按照可配置的 sleep 与 jitter 周期访问服务端，以获取待执行任务并减少明显的固定周期特征。

（3）**任务执行功能。** Agent 需要支持最基本的任务类型，包括 Shell 命令执行、文件上传下载以及进程列表查询，以验证任务调度链路的完整性。

（4）**结果回传功能。** Agent 在完成任务后应对结果进行序列化、必要加密与回传，保证控制端能够正确展示执行结果。

（5）**跨平台运行功能。** 本文选用 Go 语言实现 Agent，原因在于 Go 原生支持通过 GOOS、GOARCH 等变量进行目标平台编译[15-16]，适合 Windows、Linux、macOS 三类主流系统的统一开发。

#### 2.1.2 通信通道（C2 Profile）功能需求

Mythic 将 C2 Profile 视为 Agent 与控制端之间的通信适配层，每个 Profile 均可通过独立 Docker 容器运行，并通过 RabbitMQ 与 Mythic 内核交互[11]。因此，本文的 Profile 需满足如下需求：

（1）**HTTP/S 协议适配。** Profile 需要接收 Agent 发起的 HTTP/S 请求，将请求数据解析为 Mythic 所需的任务模型，同时将 Mythic 的响应重新封装为 Agent 可识别的数据结构。

（2）**配置化管理。** 参考 Mythic C2 Profile 的 `config.json` 设计[12]，Profile 应允许操作者在不直接修改服务端代码的情况下配置回连路径、监听地址、请求头参数、加密密钥和轮询间隔等信息。

（3）**流量伪装能力。** C2 流量往往通过 URI、请求头、User-Agent、Content-Type 等字段模拟正常 Web 通信[2][6]，因此 Profile 应支持对这些字段进行灵活配置，以增强实验环境下的可研究性。

（4）**加密与转发能力。** Profile 需要对 Agent 提交的数据进行解密、校验和转发，对下发任务进行加密封装，保证两端通信的正确性和一致性。

#### 2.1.3 非功能性需求

除功能需求外，系统还需满足以下非功能性需求：

（1）**稳定性。** Agent 应能够在长时间运行过程中保持心跳任务与任务执行能力，不因短时网络波动而直接退出。

（2）**可扩展性。** 由于 Mythic 本身采用模块化设计[10-12]，本文实现也应采用分层思想，便于后续扩展更多任务类型或替换新的通信 Profile。

（3）**轻量性。** Agent 作为受控端组件，应尽量减少运行时资源占用，避免因内存或 CPU 占用过高影响实验观察。

（4）**兼容性。** 系统应在 Windows、Linux、macOS 环境中完成编译与运行验证。

（5）**安全性。** 本文并不追求真实攻击意义上的高对抗性，但为了保证实验数据在传输过程中的基本保密性，仍需采用 JSON 序列化[17]、AES 对称加密[18] 以及 TLS 通道封装[19] 等方式实现通信保护。

### 2.2 系统业务流程分析

#### 2.2.1 植入体（Agent）上线注册流程

Agent 启动后首先加载本地配置，包括控制端地址、Profile 路径、加密参数和休眠周期等内容。随后，Agent 收集本机主机名、用户名、操作系统、IP 地址和进程标识等基础信息，构造注册报文并通过 HTTP POST 发送给 Profile 服务端。Profile 对收到的数据执行解密与字段校验后，调用 Mythic 对应接口完成 callback 注册，并向 Agent 返回注册成功状态及后续轮询参数。注册成功后，Agent 进入主循环。

#### 2.2.2 心跳通信与指令交互流程

在主循环中，Agent 按照设定的 sleep 与 jitter 周期生成轮询请求，请求中包含 callback 标识、序列号和时间戳等基本信息。Profile 接收请求后，与 Mythic 的任务队列进行交互；若存在待执行任务，则将任务列表加密后返回，否则返回空任务或保持等待状态。Agent 解密响应后，交由任务调度器逐项处理。该机制本质上是一种典型的 beaconing 模型，符合当前 Web 型 C2 在实验环境中的简化实现方式。

#### 2.2.3 任务执行与结果回传流程

当 Agent 接收到任务后，首先根据任务类型进行分发。例如，Shell 类型任务进入命令执行模块，文件类任务进入上传下载模块，进程枚举任务进入系统信息采集模块。各模块执行结束后，将结果封装为统一 JSON 格式，必要时按块切分，并在下一次或当前次通信中回传至控制端。Profile 负责对回传结果进行解密、重组和转发，最终由 Mythic 在控制台中展示。

### 2.3 本章小结

本章围绕系统建设目标，对 Agent 与 C2 Profile 的功能需求、非功能需求和业务流程进行了分析。结合 Mythic 的容器化和组件化机制，明确了本文系统以“轻量 Agent + 独立 Profile 容器 + Mythic 核心平台”为总体思路，为后续系统设计与实现奠定了基础。

---

## 3 系统设计

### 3.1 设计目标

本文系统设计遵循“可用、可扩展、可复现”的原则，具体目标包括：

1. 在不修改 Mythic 核心框架的前提下，实现独立可部署的 HTTP/S C2 Profile；
2. 设计一个支持跨平台编译与运行的轻量化 Agent；
3. 实现注册、心跳、任务执行、结果回传的完整链路；
4. 通过统一报文结构和模块化代码组织，为后续扩展更多任务或协议保留接口；
5. 保持实现复杂度适中，便于毕业设计答辩展示与实验复现。

### 3.2 总体设计

系统总体上分为三层：控制层、适配层与执行层。控制层为 Mythic 核心平台，负责任务下发、结果管理和界面展示；适配层为本文设计的 C2 Profile 容器，负责外部 HTTP/S 通信与 Mythic 内部消息之间的转换；执行层为运行在目标主机上的 Agent，负责具体任务执行。

其中，Mythic 核心与 Profile 容器之间通过消息机制和接口协同工作[10-12]，Profile 容器与 Agent 之间通过自定义 HTTP/S 报文进行通信。该设计使系统职责边界清晰：控制平台专注于任务管理，Profile 专注于协议适配，Agent 专注于任务执行。

### 3.3 C2通信协议（Profile）设计

#### 3.3.1 通信拓扑结构设计

通信拓扑采用“Agent—Profile—Mythic”三级结构。Agent 不直接与 Mythic 核心交互，而是只感知 Profile 暴露出的 HTTP/S 入口。这样做的优势在于：一方面能够降低 Agent 与控制平台的耦合度；另一方面，若后续需要将 HTTP Profile 替换为 WebSocket、DNS 或其他传输方式，仅需重写适配层，而无需大规模修改 Agent 主体逻辑。

#### 3.3.2 协议报文结构设计

为了便于调试与扩展，本文采用 JSON 作为报文数据组织方式[17]。Agent 与 Profile 之间的核心字段包括：

- `action`：动作类型，如 `checkin`、`get_task`、`post_response`；
- `id`：Agent 唯一标识；
- `seq`：消息序号；
- `timestamp`：时间戳；
- `data`：任务数据或执行结果；
- `meta`：主机信息、平台信息、分块信息等附加字段。

在实际传输前，JSON 数据先进行序列化，再由 AES 对称加密模块处理；若使用 HTTPS，则外层传输继续由 TLS 1.3 提供链路保护[19]。这种“双层保护”方式虽然在实验环境中并非唯一选择，但实现简单、便于展示，也符合 C2 加密通信的一般设计思路[4]。

#### 3.3.3 流量伪装策略设计

根据 MITRE ATT&CK 对 Web Protocols 的描述[2]以及现有 HTTP 型 C2 研究[6]，本文从以下几个方面实现流量伪装：

1. **路径伪装。** 将注册、轮询、回传接口伪装为常见 Web 资源路径，如 `/api/v1/update`、`/static/check` 等；
2. **请求头伪装。** 允许配置 User-Agent、Accept、Content-Type 等字段，使通信外观更接近常规 Web 请求；
3. **请求方法分离。** 注册与结果回传使用 POST，任务拉取可使用 GET 或 POST，增强行为多样性；
4. **时间抖动。** 在固定睡眠时间基础上增加随机扰动，避免严格等间隔心跳带来的识别特征。

#### 3.3.4 加密传输方案设计

考虑到命令回传与结果数据在网络中具有一定敏感性，本文采用 AES 对称加密算法保护报文主体[18]，在部署 HTTPS 时再叠加 TLS 1.3 作为外层传输安全机制[19]。其中，AES 密钥作为 Profile 构建参数的一部分写入 Agent 配置；报文体采用统一编码后再加密，服务端按相同密钥解密。该方案实现简单、跨平台适配方便，适用于毕业设计的原型验证场景。

### 3.4 远控代理（Agent）软件架构设计

#### 3.4.1 模块化分层设计

Agent 内部采用模块化分层结构，主要包括：配置加载模块、通信模块、任务调度模块、命令执行模块、文件模块和系统信息模块。配置加载模块负责初始化参数；通信模块负责封装、发送、接收和解密数据；任务调度模块根据任务类型进行分发；具体业务模块完成实际操作。

#### 3.4.2 任务调度器设计

任务调度器是 Agent 的控制中心。其核心思路是建立“任务类型—处理函数”的映射表，例如将 `shell` 映射到命令执行函数、将 `upload/download` 映射到文件模块、将 `ps` 映射到进程获取模块。这样既能降低 if-else 分支复杂度，也便于后续新增任务类型。

#### 3.4.3 指令解析与分发机制设计

Profile 返回的任务一般以 JSON 数组形式封装。Agent 在接收任务后，先完成整体 JSON 解析，再逐项读取 `command`、`task_id`、`params` 等字段，根据命令名选择对应处理函数。若遇到未知任务，则生成错误结果并回传控制端，以保证系统不会因单个异常任务而中断主循环。

#### 3.4.4 Shell命令执行模块设计

Shell 模块主要用于验证远程命令执行链路。为兼顾跨平台，Windows 环境调用 `cmd /C` 执行命令，Linux/macOS 环境调用 `/bin/sh -c`。执行结果包括标准输出、标准错误和退出码，并统一封装为结果对象上报控制端。

#### 3.4.5 文件上传下载逻辑设计

文件模块采用“分块传输 + 状态标记”的方式实现。下载任务中，Agent 从服务端接收文件元数据与块内容，按序写入本地文件；上传任务中，Agent 按固定块大小读取目标文件，逐块编码并回传。该方式能够降低单个请求体积过大的风险，也更便于断点续传和失败重试功能的后续扩展。

#### 3.4.6 进程列表获取模块设计

进程信息模块用于获取当前主机的基础运行态信息。本文采用跨平台兼容方式实现：优先调用系统 API 或通用库获取进程列表，在实现成本较高的平台则通过系统命令辅助采集。最终统一输出进程名、PID、所属用户和简要状态等信息，以满足实验展示需求。

### 3.5 本章小结

本章从总体架构、通信协议和 Agent 软件架构三个层面对系统进行了设计。通过将 Profile 与 Agent 解耦、将任务调度与具体功能模块解耦，系统形成了较清晰的层次结构，为后续实现与扩展提供了良好的工程基础。

---

## 4 系统实现

### 4.1 开发环境搭建

本文系统采用 Mythic 作为控制平台，利用 Docker 完成核心服务与 Profile 容器部署[10-14]。Agent 使用 Go 语言开发，原因在于 Go 在跨平台编译和静态链接方面具有明显优势[15-16]。实验环境中，控制端部署在 Ubuntu 22.04 主机，安装 Docker、docker compose 与 Mythic CLI；受控端分别准备 Windows 11、Ubuntu 22.04 和 macOS 14 测试节点，用于验证跨平台运行效果。

### 4.2 C2 Profile适配实现

#### 4.2.1 Profile Docker容器配置

依据 Mythic 的 Profile 开发方式，每个 C2 Profile 均作为独立容器运行，并通过配置文件声明通信参数[11-12]。本文为 Profile 编写 `config.json`，定义监听地址、端口、回连路径、请求方法、加密参数和默认轮询时间等配置项；同时编写 Dockerfile 与启动脚本，将 Profile 服务打包为独立镜像并接入 Mythic 实例。

#### 4.2.2 服务端监听逻辑代码实现

Profile 服务端启动后，监听指定 HTTP/S 路由，对外接收来自 Agent 的注册、轮询和回传请求。收到请求后，服务端先读取请求体并完成解密、字段校验和动作识别，再根据 `action` 类型分别处理：

- 对 `checkin` 请求，调用注册逻辑并生成 callback 标识；
- 对 `get_task` 请求，查询 Mythic 是否存在待执行任务；
- 对 `post_response` 请求，接收结果并写入控制端。

整个处理流程强调“协议转换”而非“任务执行”，即 Profile 只充当适配层角色，不直接承担攻击逻辑。

#### 4.2.3 流量转发与解密实现

Profile 在接收到密文报文后，先进行 Base64 解码，再调用 AES 解密模块恢复明文 JSON。解析成功后，将数据映射为 Mythic 所需结构并推送至控制端；在向 Agent 返回任务时，则执行相反方向的封装流程。为减少不同任务类型间的分支耦合，本文将解密、解析、转发、响应封装拆分为多个函数实现，提升了代码可维护性。

### 4.3 远控代理（Agent）核心实现

#### 4.3.1 主循环（Main Loop）与心跳机制代码实现

Agent 启动后首先执行配置初始化和注册逻辑，注册成功后进入主循环。主循环主要包含以下步骤：

1. 根据 sleep 与 jitter 计算下一次轮询时间；
2. 组装 `get_task` 请求并发送至 Profile；
3. 接收服务端响应并完成解密解析；
4. 若存在任务则交由调度器处理；
5. 汇总任务结果并执行回传。

这种循环结构简单稳定，能够直观体现 C2 系统中“轮询—执行—回传”的工作模式。

#### 4.3.2 任务接收与JSON解析实现

Agent 使用统一数据结构描述任务对象与结果对象。收到服务端响应后，先校验消息序列号和回调标识，再将 JSON 解析为任务列表。对于每个任务，调度器依据任务类型选择具体处理函数，并在执行完成后生成结构统一的结果对象。解析失败、字段缺失或未知命令等异常情况，均会转化为错误信息上报，以提高系统容错性。

#### 4.3.3 系统API调用实现

在命令执行方面，Agent 根据运行平台选择不同的系统命令解释器；在文件处理方面，调用本地文件读写接口实现分块收发；在进程获取方面，结合平台能力调用系统 API 或命令输出。通过对这些底层调用进行统一封装，Agent 上层逻辑无需关心具体平台差异，从而保证代码结构更加清晰。

### 4.4 本章小结

本章介绍了系统的具体实现过程，包括 Mythic 环境部署、Profile 容器配置、通信适配逻辑以及 Agent 核心模块实现。通过将核心逻辑拆分为多个独立模块，本文完成了一套具备基本可用性的 C2 通信原型系统。

---

## 5 系统测评

> 注：以下数据为论文初稿中的示例数据，正式提交时建议使用你自己环境中的抓包、日志和运行结果替换。

### 5.1 测试目的与环境

#### 5.1.1 测试目的

系统测评旨在验证以下问题：

1. Agent 是否能够稳定接入控制端并保持心跳；
2. C2 Profile 是否能够正确完成注册、任务转发与结果回传；
3. 核心任务功能是否能够正常执行；
4. 系统在不同操作系统下是否具备一致性；
5. 长时间运行时系统是否保持稳定。

#### 5.1.2 测试环境

表5-1 测试环境配置

| 项目 | 配置 |
|---|---|
| 控制端系统 | Ubuntu 22.04 LTS |
| Mythic版本 | Mythic 3.4 |
| 容器环境 | Docker 26.x，Docker Compose |
| Agent开发语言 | Go 1.22 |
| 测试节点1 | Windows 11 Pro 23H2 |
| 测试节点2 | Ubuntu 22.04 |
| 测试节点3 | macOS 14 |
| 网络环境 | 局域网/NAT实验环境 |

### 5.2 通信协议测试

#### 5.2.1 连通性测试

连通性测试重点验证 Agent 的注册、轮询和结果回传链路是否完整。测试方法为：在三个平台分别启动 Agent，观察其是否能够在 Mythic 控制台产生 callback 记录，并执行 30 轮连续轮询。

表5-2 连通性测试结果

| 测试项 | Windows | Linux | macOS |
|---|---:|---:|---:|
| 首次上线是否成功 | 是 | 是 | 是 |
| 平均注册耗时/s | 1.42 | 1.16 | 1.28 |
| 30轮轮询成功率 | 100% | 100% | 100% |
| 结果回传是否完整 | 是 | 是 | 是 |

测试结果表明，Agent 在三种平台下均能成功完成注册与轮询，说明 Profile 的协议转换逻辑与 Agent 的通信实现能够稳定协同工作。

#### 5.2.2 流量伪装效果测试

采用 Wireshark 对 Agent 与 Profile 之间的通信进行抓包分析，重点观察请求路径、请求头、报文体和传输层封装情况。结果显示，系统通信整体表现为常规 HTTP/S 请求，请求路径与头字段均能够根据配置进行变化；在 HTTPS 模式下，报文主体无法直接以明文形式读取，符合预期设计。对于应用层主体，即使在 HTTP 模式下，数据内容也因 AES 加密而表现为编码后的密文形式。

从实验结果看，本文系统虽然不以绕过高级检测为目标，但已具备基本的流量伪装能力，可用于后续流量识别、特征提取与检测模型实验。

### 5.3 远控代理功能测试

#### 5.3.1 基础指令执行测试

基础指令测试主要验证远程命令执行能力。测试中分别下发 `whoami`、`hostname`、`pwd`/`cd` 等命令，Agent 均能正确执行并回传标准输出。任务执行的平均往返时间约为 0.8~1.5 s，满足实验环境下的使用需求。

#### 5.3.2 文件管理功能测试

文件管理测试选取 1 MB、5 MB 与 10 MB 三种文件规模进行上传下载验证。测试结果表明，分块传输逻辑能够正确重组文件，MD5 校验结果一致。5 MB 文件在局域网环境中的平均上传耗时约为 6.1 s，平均下载耗时约为 5.8 s，说明文件模块能够满足原型系统的基本需求。

#### 5.3.3 长时间运行稳定性测试

稳定性测试中，将 Agent 的轮询间隔设为 30 s，连续运行 24 h，共产生约 2880 次轮询。测试期间仅出现少量因网络抖动导致的超时重试，但 Agent 未出现崩溃或失联现象，综合轮询成功率达到 99.8% 以上。该结果说明主循环与错误处理机制具有较好的稳定性。

### 5.4 跨平台运行测试

#### 5.4.1 Windows环境测试结果

Windows 平台下，Agent 能够正常完成注册、轮询、命令执行、文件操作和进程查询。使用 Go 交叉编译得到的可执行文件可直接运行，无需额外依赖，体现了 Go 在跨平台部署中的优势[15-16]。

#### 5.4.2 Linux/macOS环境测试结果

Linux 与 macOS 环境下，Agent 同样能够稳定接入控制端并完成主要任务。由于底层命令解释器差异，部分命令参数存在平台差别，但并不影响整体功能实现。实验结果表明，本文采用的“统一调度 + 平台适配”策略能够较好地满足跨平台需求。

### 5.5 测试结果分析

综合测试结果可得：

1. **系统功能完整。** 注册、轮询、任务执行和结果回传链路均可稳定工作；
2. **通信设计合理。** JSON + AES + HTTP/S 的组合既保证了实现复杂度可控，也满足了实验型 C2 原型的基本需求；
3. **模块划分清晰。** Profile 与 Agent 的职责分离降低了系统耦合度，便于调试和扩展；
4. **跨平台效果较好。** 基于 Go 的 Agent 在三种主流平台中均能稳定运行；
5. **仍存在改进空间。** 当前系统的任务类型较少，异常恢复、断点续传、动态密钥交换和更高强度的流量伪装能力仍有提升余地。

### 5.6 本章小结

本章从协议连通性、流量伪装、功能完整性、稳定性和跨平台兼容性等方面对系统进行了测评。测试结果表明，本文所设计实现的 C2 原型系统能够满足毕业设计的基本研究目标，并具备进一步扩展的工程基础。

---

## 6 结论与展望

### 6.1 结论

本文围绕 Mythic 框架下的 Agent 与 C2 Profile 设计与实现开展研究，删除传统“相关技术”单独成章的写法，将 Docker 容器化、Mythic 组件化架构、HTTP/S 通信、JSON 序列化、AES/TLS 传输保护和 Go 跨平台编译等内容融入需求分析与系统设计过程之中，形成了一套较为完整的毕业设计实现方案。

通过本文工作，完成了以下目标：

1. 完成了基于 Mythic 的 C2 原型系统总体方案设计；
2. 实现了独立部署的 HTTP/S Profile 容器；
3. 实现了支持 Windows、Linux、macOS 的轻量化 Agent；
4. 完成了注册、心跳、命令执行、文件传输和进程查询等核心功能；
5. 通过实验验证了系统的可用性、稳定性和跨平台兼容性。

总体来看，本文系统能够较好地支撑教学实验和授权研究场景下的 C2 通信原型验证，具有一定的研究意义与实践价值。

### 6.2 展望

尽管本文已完成原型系统设计与实现，但仍存在进一步改进空间：

1. **丰富任务类型。** 后续可增加屏幕截取、系统信息采集、端口转发等更多实验模块；
2. **增强通信机制。** 可进一步研究基于 WebSocket、DNS 或消息中间件的多种 Profile 适配方式；
3. **优化安全设计。** 可引入动态密钥协商、证书双向认证和更细粒度的数据完整性校验；
4. **完善工程能力。** 可加入断点续传、失败重试、日志持久化和插件化任务管理机制；
5. **支持防御研究。** 可将本系统生成的流量作为样本，继续开展 C2 流量检测、特征提取和异常识别研究。

---

## 参考文献

[1] MITRE ATT&CK. Command and Control, Tactic TA0011[EB/OL]. https://attack.mitre.org/tactics/TA0011.

[2] MITRE ATT&CK. Application Layer Protocol: Web Protocols, Sub-technique T1071.001[EB/OL]. https://attack.mitre.org/techniques/T1071/001.

[3] MITRE ATT&CK. Ingress Tool Transfer, Technique T1105[EB/OL]. https://attack.mitre.org/techniques/T1105.

[4] MITRE ATT&CK. Encrypted Channel, Technique T1573[EB/OL]. https://attack.mitre.org/techniques/T1573.

[5] GARDINER J, COVA M, NAGARAJA S. Command & Control: Understanding, Denying and Detecting—A Review of Malware C2 Techniques, Detection and Defences[EB/OL]. arXiv:1408.1136, 2014.

[6] ACARALI D, RAJARAJAN M, KOMNINOS N, et al. Survey of approaches and features for the identification of HTTP-based botnet traffic[J]. Journal of Network and Computer Applications, 2016, 76: 1-15.

[7] CHATZOGLOU E, KAMBOURAKIS G. C3: Leveraging the Native Messaging Application Programming Interface for Covert Command and Control[J]. Future Internet, 2025, 17(4): 172.

[8] 梁松林, 林伟, 王珏, 等. 面向后渗透攻击行为的网络恶意流量检测研究[J]. 计算机工程, 2024, 50(5): 128-138.

[9] 吴沛颖, 李晓慧, 王俊峰. 基于上下文感知语言模型的C2流量检测研究[J]. 计算机工程, 2025. DOI:10.19678/j.issn.1000-3428.0070472.

[10] its-a-feature. Mythic: A collaborative, multi-platform, red teaming framework[EB/OL]. https://github.com/its-a-feature/Mythic.

[11] Mythic Documentation. Installation[EB/OL]. https://docs.mythic-c2.net/installation.

[12] Mythic Documentation. C2 Profiles Overview[EB/OL]. https://docs.mythic-c2.net/operational-pieces/c2-profiles.

[13] 周伟平, 杨维永, 王雪华, 等. 面向工业控制系统的渗透测试工具研究[J]. 计算机工程, 2019, 45(8): 92-101.

[14] JARAMILLO D, NGUYEN D V, SMART R. Leveraging microservices architecture by using Docker technology[C]//SoutheastCon 2016. IEEE, 2016: 1-5.

[15] The Go Programming Language. Installing Go from source[EB/OL]. https://go.dev/doc/install/source.

[16] The Go Programming Language. Go Wiki: Building Windows Go programs on Linux[EB/OL]. https://go.dev/wiki/WindowsCrossCompiling.

[17] BRAY T. The JavaScript Object Notation (JSON) Data Interchange Format[S]. RFC 8259, 2017.

[18] NIST. FIPS 197: Advanced Encryption Standard (AES)[S]. 2023 update.

[19] RESCORLA E. The Transport Layer Security (TLS) Protocol Version 1.3[S]. RFC 8446, 2018.

---

## 附录A 核心代码片段（建议）

- A.1 Agent 主循环代码
- A.2 AES 加解密模块代码
- A.3 任务分发器代码
- A.4 文件分块传输代码

> 如果你准备继续完善正文，我建议下一步直接把你自己的实际环境、测试截图、Wireshark 抓包图和核心代码贴进去，这样这篇论文就能从“初稿”变成“可提交版”。
